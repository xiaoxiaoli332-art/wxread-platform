{
  "name": "wxread-sync-server",
  "version": "1.0.0",
  "description": "微信读书管理平台 —— 后端/同步服务（零依赖 Node，last-write-wins 共享库）",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=14"
  }
}
